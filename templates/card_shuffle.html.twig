{% extends "base.html.twig" %}

{% block title %}Cards Shuffled{% endblock %}
{% block body %}
{% for card, color in cardView %}
{% if color == "Red" %}
            <div class="cards" style="color:red">{{ card }}</div>
{% else %}
    <div class="cards">{{ card }}</div>
            {% endif %}
{% endfor %}

</p>
<div class="options">
    <li><a href="{{ path('card_shuffle') }}">Blanda igen</a></li>
    <li><a href="{{ path('card_draw_num', {num: 1 }) }}">Ta upp det översta kortet i leken </a></li>
    <li><a href="{{ path('card_draw_num', {num: 5 }) }}">Ta upp de fem översta korten i leken </a></li>
</div>
{% endblock %}

